name: Rappido
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  form_field_validator: ^1.1.0
  provider: ^6.1.2
  firebase_auth: ^5.3.2
  firebase_core: ^3.7.0
  google_fonts: ^6.2.1
  flutter_expandable_fab: ^2.3.0
  firebase_database: ^11.1.5
  esc_pos_utils: ^1.0.0
  permission_handler: ^11.3.1
  pdf: ^3.8.4
  esc_pos_bluetooth: ^0.4.1
  open_file: ^3.5.9
  flutter_blue_plus: ^1.34.4
  shared_preferences: ^2.3.3
  flutter_launcher_icons: ^0.11.0
  fluttertoast: ^8.2.8

  path_provider: any
  http: ^1.2.1
  flutter_test:
    sdk: flutter
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon.png" # Path to your app icon

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
#    - assets/stamp2.png
#    - assets/stamp.png